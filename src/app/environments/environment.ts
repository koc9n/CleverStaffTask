export const environment = {
  production: false,
  /** Replace with your actual API key
   * Get it from https://app.ipgeolocation.io/
   * Register as Individual it gives free 1000 requests to API , should be enough for test
  */
  apiKey: 'YOUR_API KEY'
};
